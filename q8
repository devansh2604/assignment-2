#include <iostream>
using namespace std;

int main() {
    int arr[10] = {2, 5, 3, 2, 3, 7, 5, 9, 9, 1};
    int n = 10, distinct = 0;
    for (int i = 0; i < n; i++) {
        bool seen = false;
        for (int j = 0; j < i; j++)
            if (arr[i] == arr[j]) { seen = true; break; }
        if (!seen) distinct++;
    }
    cout << "Distinct count: " << distinct;
    return 0;
}
