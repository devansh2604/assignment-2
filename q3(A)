#include <iostream>
using namespace std;

int main() {
    int arr[5] = {1,2,3,5,6}; // 1..6 missing 4
    int n = 5, i;
    for (i = 0; i < n; i++) {
        if (arr[i] != i + 1) { cout << "Missing: " << i + 1; return 0; }
    }
    cout << "No missing";
    return 0;
}
