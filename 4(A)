#include <iostream>
#include <string.h>
using namespace std;

int main() {
    char a[100], b[100];
    cout << "Enter first: ";  cin >> a;
    cout << "Enter second: "; cin >> b;
    strcat(a, b);
    cout << "Concatenated: " << a;
    return 0;
}
