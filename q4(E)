#include <iostream>
using namespace std;

int main() {
    char s[100]; cout << "Uppercase string: "; cin >> s;
    for (int i = 0; s[i] != '\0'; i++)
        if (s[i] >= 'A' && s[i] <= 'Z') s[i] = s[i] + 32;
    cout << "Lowercase: " << s;
    return 0;
}
